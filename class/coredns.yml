parameters:
  kapitan:
    compile:
      - input_paths:
          - coredns/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - coredns/component/main.jsonnet
        input_type: jsonnet
        output_path: coredns/
